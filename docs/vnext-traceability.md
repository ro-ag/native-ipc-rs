# vNext normative traceability matrix

This matrix is the release ledger for the normative requirements in
[`native-ipc-vnext-spec.md`](native-ipc-vnext-spec.md). A test name beginning
with `planned::` does not exist yet and is not evidence. Native evidence is
recorded only for execution on the named OS/architecture; compilation and
cross-compilation never change a target from `unverified` to `verified`.

Evidence states: `planned`, `implemented`, `verified`, `unverified`, `blocked`,
and `n/a`.
The exact release commit SHA and CI run URL must replace every `planned` or
`unverified` cell before release.

Target abbreviations: `Lx` = Linux GNU AMD64/Arm64, `Ma` = macOS Arm64,
`Wx` = Windows AMD64/Arm64, and `All` = all five native targets.

| ID | Normative MUST / MUST NOT | Owning module or document | Positive test/evidence | Negative test/evidence | Native target evidence |
| --- | --- | --- | --- | --- | --- |
| R2.1 | Safe allocation is anonymous and zeroed; library views exclude execute and delegated authority matches the documented target limit. | `native-ipc::region`, private `backend::*` | `planned::native::allocation_zero_authority` | Linux isolated RX-alias and existing-map-upgrade probes; other targets planned | Lx mechanism characterized; production MDWE binding unimplemented; Ma/Wx unverified |
| R2.2 | Initialization is quiescent before capability escape. | `native-ipc::region` typestates | `planned::ui::private_initialize` | `planned::ui::prepared_has_no_payload` | portable planned; All unverified |
| R2.3 | Writer direction is a consuming per-region choice. | `native-ipc::region` | `planned::api::both_writer_directions` | `planned::ui::private_reuse_after_prepare` | All unverified |
| R2.4 | Region handles are opaque and application-neutral. | facade public API | `planned::api::opaque_region_handle` | `planned::api::no_native_or_app_types` | portable planned |
| R2.5 | Bootstrap authenticates the library-owned exact child. | `session`, private `backend::*::process`; Linux `spawn::{UnauthenticatedLinuxSpawn,NegotiatingLinuxSpawn,CoordinatorAcceptedEvidenceOwner,ReceiverAcceptedEvidenceOwner}` plus role-scoped backend evidence | coordinator evidence inseparably owns the exact lifecycle pidfd, held child image, accepted endpoint/transcript, and identity facts; receiver evidence owns only authenticated spawning-parent endpoint/transcript facts and explicitly no coordinator-owned image/pidfd proof | wrong role/nonce/target/atomic/limit/reserved/truncated/rights records and credential-changing executable modes reject; mismatched role evidence and unaccepted/coordinator-only/rejected/poisoned/replayed transcript facts cannot mint evidence | exact implementation SHA `a63a06ea34fdb2389abe1791bc636741b3018e15` is green on native Linux AMD64/Arm64 and ASan in [Actions 29202485515](https://github.com/ro-ag/native-ipc-rs/actions/runs/29202485515); private role-asymmetric evidence only, with physical Arm64, macOS/Windows Phase 5i-F, public session/Ready/authority, and release evidence unverified |
| R2.6 | A heterogeneous batch contains 1 through 16 regions. | `batch` | `planned::batch::counts_1_2_4_16` | `planned::batch::counts_0_17_rejected` | All unverified |
| R2.7 | A batch permits arbitrary writer-direction mixtures. | `batch` | `planned::batch::all_direction_profiles` | `planned::batch::wrong_direction_rejected` | All unverified |
| R2.8 | One canonical manifest and READY/COMMIT covers the complete batch. | `wire::manifest`, `batch` | `planned::batch::one_barrier_full_transcript` | `planned::batch::partial_or_mutated_transcript` | All unverified |
| R2.9 | Runtime visibility is all-or-nothing at both endpoints. | `batch::PendingBatch`, `active` | `planned::batch::complete_activation` | `planned::batch::nth_failure_exposes_none` | All unverified |
| R2.10 | Runtime copies are checked/allocation-free and pointer access is explicitly unsafe. | `active`, feature `raw-pointer` | `planned::rt::checked_copy_contract` | `planned::ui::safe_pointer_unavailable` | All unverified |
| R2.11 | Session owns exit observation, poison, termination, reap, and cleanup. | `session`, `cleanup` | `planned::lifecycle::owned_child_close` | `planned::lifecycle::exit_each_state` | All unverified |
| R2.12 | Public API/runtime semantics are identical; native authority limits are target-explicit. | facade API plus backend conformance | `planned::api::target_surface` | `planned::ci::unsupported_target_compile_fail` | All unverified |
| R2.13 | Application control is bounded, authenticated, and non-real-time. | private `control` codec, `backend::accepted_control::AcceptedControlDispatcher`, and Linux role-scoped accepted transports; future public Ready session | private Linux empty/exact-65,464-byte duplex control under caller absolute deadlines with exact per-record credentials, one bounded `recvmsg`, independent sequences, transaction exclusion, and coordinator-only owned-child lifecycle | local max+1/reserved-kind rejection before I/O; injected rights are adopted/closed before rejection; wrong credentials, truncation, replay, silence/deadline, live empty record, post-consumption poll failure, ambiguous I/O, poll failure, and transaction conflict persistently poison | exact G1a `a26e1df71fccf44540a174a4994cd421668b5700` is green across all ten jobs in [Actions 29204483739](https://github.com/ro-ag/native-ipc-rs/actions/runs/29204483739); private Linux only, with no public Ready/session/control API, batch capability transfer, or All native release evidence |
| R2.14 | Every claimed security property has native adversarial tests. | `tests/native`, CI | exact-release conformance jobs | release gate rejects missing native result | All unverified |
| R2.15 | Ordinary users do not import OS modules or manipulate native names/handles. | facade exports; feature `raw-handle` | `planned::api::platform_neutral_flow` | `planned::ui::native_parts_not_safe` | portable planned |
| R3.1 | Public memory/transport contains no forbidden application/fixed-four vocabulary. | facade API and docs | `planned::api::application_neutral` | `planned::lint::forbidden_vocabulary` | portable planned |
| R3.2 | Base manifest is independent of core layouts/schemas/acks. | `wire::manifest`; Cargo graph | `planned::wire::generic_manifest` | `planned::graph::manifest_has_no_core_dependency` | portable planned |
| R4.1 | Hostile peer cannot trigger trusted UB/OOB. | `active`, all parsers/backends | Miri + native hostile corpus | truncation/arithmetic/mutation corpus | All unverified |
| R4.2 | Hostile peer cannot create undocumented writable aliasing. | region typestates; backend permission checks | `planned::native::designated_writer_succeeds` | `planned::native::reader_store_and_map_fail` | All unverified |
| R4.3 | Hostile peer cannot trigger use-after-free/double cleanup/reap. | RAII `cleanup::Ledger`; private Linux exact-child lifecycle | Linux live/stalled Drop, cancel, auto-reap, exact terminate/reap, and baseline cycles | Linux injected persistent signal/poll/reap terminal faults park a durable exact-pidfd owner without retrying or losing ownership; portable and other-target permutations planned | private Linux mechanism exercised bare-metal AMD64 at `e904e35`; public session and All release targets unverified |
| R4.4 | Hostile peer cannot cause trusted-process executable shared mappings or obtain reader write authority; Linux direction-specific peer execute authority remains explicit. | private backends | Linux exact native RX-alias/upgrade/outside-tree-delegate probes | coordinator writer removal, later-write-map denial, MDWE-tree upgrade denial, and production fail-closed memory preparation | Linux residual authority, including outside-tree pre-seal delegation, characterized bare-metal AMD64 at `e904e35`; production receipt binding, physical Arm64 release evidence, and Ma/Wx remain unverified |
| R4.5 | Stale/replayed/substituted/foreign transactions never activate. | `wire::Provenance`, `batch` | `planned::replay::fresh_transaction` | `planned::replay::session_and_tx_substitution` | All unverified |
| R4.6 | Uncommitted batches never expose partial runtime regions. | `PendingBatch` | `planned::batch::activation_after_commit` | `planned::batch::failure_first_middle_last` | All unverified |
| R4.7 | Hostile traffic cannot extend an absolute deadline. | `deadline`, transport loops | private Linux immediate packet/accept success under one deadline | real listener receives distinct credential-bearing hostile-child connections through the original deadline; wrong packets terminate immediately; silence, saturated output, EINTR, late completion, and already-expired accept | corrected test at exact commit `3721cd3` is green on hosted native Linux AMD64/Arm64 and ASan in [Actions 29195512981](https://github.com/ro-ag/native-ipc-rs/actions/runs/29195512981); physical-host rerun, public session, and other targets unverified |
| R4.8 | Covered failure paths do not leak resources. | `cleanup::Ledger`, fault boundary | private Linux fd/task/process/zombie/mapping/directory baselines and stress cycles | malformed ancillary, pre-exec, cancel, deadline, and auto-reap restore baselines; persistent terminal cleanup faults retain the exact pidfd until isolated process exit and are excluded from restoration claims | private Linux source-tree coverage exercised bare-metal AMD64 through `98b7163`; complete Nth-operation and All release-target evidence unverified |
| R4.9 | Exactly one endpoint has library-managed store authority; the other is kernel-read-only; library views exclude execute and target residual authority is exact. | typestates and private backends | mixed-direction native permission suite | opposite endpoint write/permission-upgrade probes plus Linux isolated RX-alias characterization | Lx mechanism characterized; production binding unimplemented; Ma/Wx unverified |
| R4.10 | Delegation cannot increase the bounded native authority delivered; Linux delivery includes RX aliases and receiver-writer pre-seal delegation outside the MDWE tree, while every delegate remains one receiver principal. | backend attenuated capability | Linux pre-seal fd import outside the MDWE tree retains the already-authorized RW mapping after final seals | coordinator writer is absent; later RW mappings are denied; no second authority principal is claimed | Linux accepted limit characterized bare-metal AMD64 at `e904e35`; authenticated production import and other targets unverified |
| R4.11 | Documentation makes no payload-integrity claim against the authorized writer. | threat model, active API docs | torn-hostile-byte contract review | forbidden-claim scan for integrity/seqlock/checksum claims | docs planned |
| R4.12 | Documentation makes no confidentiality claim against an authorized reader. | threat model, region API docs | authority-scope review | forbidden-claim scan for confidentiality claims | docs planned |
| R4.13 | Documentation makes no revocation claim after capability delivery. | threat model, abort/cleanup docs | explicit non-revocation examples | forbidden-claim scan for revoke/erase claims | docs planned |
| R4.14 | Documentation makes no one-PID confinement or peer-copy erasure claim. | threat model, process docs | authority-principal/delegation review | forbidden-claim scan for PID confinement/erasure | docs planned |
| R4.15 | Documentation makes no rollback or distributed crash-atomicity claim. | batch atomicity and poisoning docs | API-visibility atomicity review | forbidden-claim scan for rollback/crash atomicity | docs planned |
| R5.1 | `RegionId(0)` and duplicate IDs fail; each object gets an unguessable incarnation. | `identity`, `batch`, CSPRNG | `planned::identity::fresh_incarnations` | `planned::identity::zero_duplicate_id` | portable + All unverified |
| R5.2 | Limits have finite secure defaults and every count/size/total/frame is checked before allocation/import. | `limits`, `checked`, parsers | boundary property tests | zero/overflow/above-hard-max corpus | portable planned; All unverified |
| R5.3 | Active leases remain charged until mapping drop. | private `liveness::ResourceOwner` + `active::{LeasedReadOwner,LeasedWriteOwner}` | `active_mapping_retains_shared_liveness_until_mapping_drop_finishes`; `active_writer_retains_the_same_exact_resource_lease` | exact-length mismatch/poison rollback, concurrent drop/close, and destructor-panic corpus | portable ownership/accounting implemented; native activation adapters unverified |
| R5.4 | `PrivateRegion` is unique, writable, anonymous, non-`Clone`, and only exposes scoped initialization. | `region::PrivateRegion` | `planned::api::scoped_initialize` | compile-fail clone/escape tests | portable planned; All unverified |
| R5.5 | Preparation consumes private state; failed preparation consumes and destroys it. | `region::prepare` | `planned::region::prepare_consumes` | faulted prepare + compile-fail reuse | All unverified |
| R5.6 | Prepared/pending values are non-Clone/non-Copy and payload-inaccessible; Drop closes all state. | typestates, cleanup | trait assertions + drop baseline | compile-fail clone/access + injected drop | All unverified |
| R5.7 | Safe code cannot derive writer authority from a reader or duplicate a sole writer. | `active` | intended Send/Sync assertions | compile-fail mutation/clone/sync | portable planned |
| R5.8 | Allocation rejects zero/overflow, rounds to pages, zeros payload/padding, and creates no executable library view. | `region`, backend allocation | page-boundary properties + zero checks | zero/overflow + platform authority probes | All unverified |
| R5.9 | Page padding is zero before transfer and inaccessible to safe runtime calls. | `region`, `active` | `planned::native::zero_padding` | safe OOB access rejection | All unverified |
| R5.10 | Clearing docs do not claim erasure of kernel/peer copies. | API docs, threat model | documentation review | forbidden-claim scan | docs planned |
| R5.11 | Safe active access provides checked read/write/fill equivalents. | `active` | offset/range success corpus | OOB/overflow/access-direction corpus | portable planned; All unverified |
| R5.12 | After activation/prefault access is allocation/syscall/lock/wait/log/panic free and bounded. | `active`, RT instrumentation | `planned::rt::negative_instrumentation` | hostile bytes/peer death during access | All unverified |
| R5.13 | External-memory soundness boundary is documented/reviewed; safe copies use volatile or audited FFI, not ordinary shared references. | `active::external_memory` safety module | Miri boundary tests + independent review | source lint rejects ordinary copy/shared slice | portable planned; All unverified |
| R5.14 | Safe runtime APIs return no persistent slices or typed references. | facade public API | API surface test | compile-fail slice/reference acquisition | portable planned |
| R5.15 | Advanced pointer access exists only as unsafe API with full bounds/alignment/init/lifetime/alias/sync/atomic/peer contract. | feature `raw-pointer`, docs | `planned::api::unsafe_pointer_feature` | unavailable without feature + compile-fail safe call | portable planned |
| R5.16 | Explicit off-thread prefault/touch reports bounded coverage without promising permanent residency. | `active::prefault` | coverage/fault observation | invalid range/required lock unavailable | All unverified |
| R5.17 | Raw native import/export is feature-gated unsafe and ownership-explicit. | feature `raw-handle` | feature API tests | default-build compile-fail/safe-call fail | All unverified |
| R5.18 | No regular-file, named POSIX SHM, or System V fallback exists. | backend selection | native object-type checks | unsupported mechanism fails closed | All unverified |
| R6.1 | Spawn uses a resolved executable, authenticates exact process, and retains race-resistant lifecycle handle. | `session::spawn`, private backend facade; Linux `process::{HeldExecutable,ExactChildLifecycle}` plus private `spawn::{UnauthenticatedLinuxSpawn,CoordinatorAcceptedEvidenceOwner}` | Linux held-inode `execveat`, clone-time pidfd, child-side `setsid`, complete-table CLOEXEC plus one dynamic bootstrap slot, exact MDWE/error-pipe state machine, nonblocking Drop, and coordinator evidence inseparably retaining exact lifecycle/image/channel/transcript identity | relative/symlink/magic/nonfile/nonexec/foreign-ELF/wrong-image/already-reaped/silent-death child; duplicate reserved environment, NUL, occupied fd slots, injected close-range/bootstrap-fd/setsid/MDWE/exec failure, malformed/partial error, deadline, auto-reap/broad waiter, and descendant survival; the tested live-deadline evidence-construction error returns only after exact child absence, fd baseline, and ECHILD; deadline/terminal-incomplete cleanup retains durable exact pidfd/child authority, while only a successfully completed reaper retires authority-free | exact implementation SHA `a63a06ea34fdb2389abe1791bc636741b3018e15` is green on native Linux AMD64/Arm64 and ASan in [Actions 29202485515](https://github.com/ro-ag/native-ipc-rs/actions/runs/29202485515); no numeric PGID signal, symmetric image proof, or ongoing image attestation; stronger descendant containment, public session composition, physical Arm64, and release evidence remain unverified |
| R6.2 | `SessionOptions` requires an expected executable identity policy on every platform. | `session::identity` | policy-required exact-image spawn | missing/mismatched policy rejection | All unverified |
| R6.3 | macOS/Windows compare stable image identity before/after spawn and hold replacement-denying rights where available. | private process backends | stable identity match | path replacement/image mismatch | Ma/Wx unverified |
| R6.4 | HELLO then ACCEPT/REJECT completes before transfer. | private `negotiation` challenged transcript + Linux `spawn::{NegotiatingLinuxSpawn,ReceiverDecisionPending,AcceptedLinuxSpawn,AcceptedLinuxReceiver}` + role-scoped accepted evidence + future session typestate | portable fresh challenged Coordinator decision and exact echoed Receiver decision; one-shot accepted transcript facts require bilateral ACCEPT; Linux bilateral ACCEPT at empty/1/exact-65,312-byte HELLO payloads and both clean Reject paths | portable zero/wrong/replayed/substituted/out-of-order challenge and decision corpus; accepted-fact extraction persistently rejects unaccepted, coordinator-only, rejected, poisoned, and replayed states; Linux prequeued decisions, every effective-field mutation, malformed/rights/silence/exit/re-exec/deadline cleanup corpus | exact implementation SHA `a63a06ea34fdb2389abe1791bc636741b3018e15` is green across all ten jobs in [Actions 29202485515](https://github.com/ro-ag/native-ipc-rs/actions/runs/29202485515), including native Linux AMD64/Arm64 and ASan; private evidence owners are Send, non-Sync, non-Clone, and expose no raw parts; public Ready/session/control/fd transfer/batch/mapping/memory authority, physical Arm64, packaged-crate, and non-Linux Phase 5i-F implementations remain unverified |
| R6.5 | Session carries bounded opaque authenticated duplex control frames. | private `control` codec + accepted dispatcher + Linux `CoordinatorLinuxControlTransport`/`ReceiverLinuxControlTransport` + future public Ready session | consumed role-scoped Accepted evidence constructs private control: coordinator inseparably owns the singular lifecycle pidfd, held image, evidence, endpoint, and `OwnedChildLifecycle`; receiver owns spawning-parent evidence and endpoint only; exact credential-bound empty/exact-65,464-byte duplex records preserve a queued valid preconstruction record | zero `SCM_RIGHTS` is required and every injected installed fd is adopted/closed before rejection; live empty seqpacket is malformed while actual HUP EOF is exit; wrong credentials, truncation/oversize/replay/silence and post-consumption diagnostic-poll EINTR are terminal without a second `recvmsg`; poison persists | exact G1a `a26e1df71fccf44540a174a4994cd421668b5700` is green across all ten jobs in [Actions 29204483739](https://github.com/ro-ag/native-ipc-rs/actions/runs/29204483739); receiver uses socket HUP and reports only `ExitedUnknown`, with no parent pidfd/image/exit-code proof; public Ready/session/control, native batch `SCM_RIGHTS`, transaction/mapping/memory, physical Arm64, packaged/release evidence unverified |
| R6.6 | `AtomicCapabilities` reports lock-free cross-process 32/64-bit atomics and alignments. | `session::AtomicCapabilities` + private `backend::linux_vnext::discover_atomic_capabilities` | compiler `target_has_atomic` gates; runtime sysconf page/cache-line match; isolated MAP_SHARED u32/u64 Release/Acquire publication+ack | unavailable/zero/non-power-of-two/under-aligned/overflow facts and required unsupported width reject; exact child/fd/map baselines | exact commit `d3ee93b` is green on all ten hosted jobs, including native Linux AMD64/Arm64 and ASan, in [Actions 29197989720](https://github.com/ro-ag/native-ipc-rs/actions/runs/29197989720); private Linux mechanism only, with no HELLO/session/receipt authority; Ma/Wx evidence and physical Arm64 release evidence unverified |
| R6.7 | `try_close` returns live ownership while leases exist and does not silently kill a serving peer. | private `liveness::ResourceOwner`; future `session::try_close` | `leases_charge_exact_limits_and_block_recoverable_close`; close after mapping destructor | concurrent and blocked mapping destructor keep close recoverably blocked | portable accounting scaffold implemented; public session/peer lifecycle and All native targets unverified |
| R7.1 | One consuming transaction supports 1..=16 arbitrary mixed directions. | `batch::TransferBatch` | 1/2/4/16 direction corpus | 0/17/wrong-direction corpus | All unverified |
| R7.2 | Callers cannot assemble independent pending tokens; transaction owns all pending/provenance/cleanup state. | private `batch::Transaction` | transaction-owned success | compile-fail provenance/pending construction | portable planned |
| R7.3 | Manifest contains no pointer/usize/raw handle/repr-Rust/application schema/padding structures and binds exact trusted platform authority facts. | `wire::manifest` manual codec + session authority witness | golden fixed-width vectors | forbidden field/reserved-byte/authority mutations | application-neutral codec implemented; session authority binding unimplemented |
| R7.4 | Object identity, size, access, and ordinal reject reorder/substitution. | backend import + manifest validator | canonical order import | reordered/substituted/wrong metadata corpus | All unverified |
| R7.5 | READY/COMMIT binds the exact full manifest. | `wire::transcript`, `batch` | exact transcript barrier | every-field mutation/replay | All unverified |
| R7.6 | Continuous malformed/wrong-peer traffic cannot extend deadline; partial ambiguous transmission poisons. | `session::AbsoluteDeadline`; private `backend::linux_vnext::ProcessBoundEndpoint`; future transports | one deadline covers injected EINTR plus send/receive, accept, lifecycle, and actual child exchange | real listener receives distinct credential-bearing hostile-child connections through the original deadline without an accept callback beginning after expiry; malformed/wrong packets terminate immediately; late send/receive return distinct ambiguity and persistently poison endpoint; expired accept performs no accept attempt | corrected test at exact commit `3721cd3` is green on hosted native Linux AMD64/Arm64 and ASan in [Actions 29195512981](https://github.com/ro-ag/native-ipc-rs/actions/runs/29195512981); physical-host rerun, public session, and all target adapters unverified |
| R7.7 | Native I/O/waits are nonblocking/pollable/cancellable and recompute remaining absolute time. | private `AuthenticatedControl` contract + `backend::linux_vnext` poll and exact-pidfd lifecycle loops | immediate packet success, actual child exchange, exact-child wait/terminate, and Drop handoff | silence, saturated `POLLOUT`, injected EINTR, pidfd exit wake, late completion, cancellation, and terminal cleanup faults | private Linux mechanism exercised bare-metal AMD64 at `e904e35`; public authenticated adapter and Ma/Wx unverified |
| R8.1 | Linux bootstrap is inherited anonymous `AF_UNIX SOCK_SEQPACKET`, not a filesystem path. | private `backend::linux_vnext::{SeqPacketEndpoint,spawn::{UnauthenticatedLinuxSpawn,NegotiatingLinuxSpawn}}` | dynamically selected inherited slot survives held exec and carries exactly two canonical zero-rights HELLOs | duplicate reserved environment/occupied slots, malformed frames, and injected rights reject; every installed/held/error/original-pair fd is owned or CLOEXEC | private composition is green at exact commit `0ae24f9` on hosted native Linux AMD64/Arm64 and ASan in [Actions 29199137594](https://github.com/ro-ag/native-ipc-rs/actions/runs/29199137594); receipt/ACCEPT and physical Arm64 release evidence unverified |
| R8.2 | Linux does not use cached pre-spawn `SO_PEERCRED`; each packet has exact post-exec credentials checked against pidfd/PID/UID/GID. | private `backend::linux_vnext` + `spawn::NegotiatingLinuxSpawn` + future authenticated adapter | both HELLO directions validate kernel automatic credentials; root-only disposable split proves real UID/GID 65534 are used while effective IDs remain 0 | wrong directional credentials/rights reject; set-user-ID/set-group-ID images reject before clone | normal bidirectional credentials are green at exact commit `0ae24f9` on hosted native Linux AMD64/Arm64 and ASan in [Actions 29199137594](https://github.com/ro-ag/native-ipc-rs/actions/runs/29199137594); the privileged differing-real/effective-ID characterization remains Arm64 seccomp-unconfined Docker-only; receipt and physical Arm64 release evidence unverified |
| R8.3 | Linux uses `MFD_NOEXEC_SEAL`, inherited irreversible MDWE, and exact size/execute/grow/shrink/future-write/seal ordering; direction-specific peer execute authority remains explicit. | private `backend::linux_vnext::{process,memory}`; future combined session authority receipt; `vnext-feasibility.md` | exact seal ordering, post-exec/inherited MDWE, and outside-tree pre-seal delegate characterization | prior exact RX-alias/upgrade runs; pre-exec failure baseline; coordinator-writer removal; later write-map denial; synthetic receiver claim mints no authority | private mechanism and accepted residual authority exercised bare-metal AMD64 at `e904e35`; no witness is minted and production memory remains fail-closed pending authenticated composition and physical Arm64 release evidence |
| R8.4 | Linux receives exact ancillary records and immediately owns every installed fd, including all malformed/error paths. | private `backend::linux_vnext::SeqPacketEndpoint` | exact 0/1/2/16-fd packets with `MSG_CMSG_CLOEXEC` | truncated payload/control, wrong peer/count, duplicate/empty/negative/wrong-level/wrong-type/short/long rights, and complete fd plus trailing byte all restore baseline | ownership primitive and malformed corpus exercised bare-metal AMD64 at `e904e35`; batch binding and physical Arm64 release evidence unverified |
| R8.5 | Intel macOS fails compilation. | facade target gate | Arm64 check | x86_64-apple-darwin compile-fail | CI planned |
| R8.6 | macOS control abstraction is replaceable by signed XPC adapter without public API change. | private `backend::macos::ControlTransport` | alternate mock transport conformance | no backend type leaks publicly | Ma unverified |
| R8.7 | macOS entries/mappings exclude execute, enforce complementary maximum rights, and ledger every installed right exactly once. | `backend::macos::{region,channel}` | reader/writer/cleanup native probes | write/execute upgrade, malformed extra-right, port-baseline corpus | Ma unverified |
| R8.8 | Windows uses exact non-inheritable duplicate access and never `DUPLICATE_SAME_ACCESS`. | `backend::windows::region` | exact reader/writer access probes | excess/wrong access import rejection | Wx unverified |
| R8.9 | Windows ledgers remote duplicates but never closes an untrusted numeric remote handle after resume; ambiguity tears down the owned Job/process. | `backend::windows::{batch,process}` | contained successful close | handle reuse/partial duplicate/ambiguous failure Job teardown | Wx unverified |
| R8.10 | Windows private pipe has explicit minimal logon-SID DACL, one-instance/local-only policy, exact endpoint PID, cancellation, and absolute deadlines. | `backend::windows::channel` | authenticated bounded framing | wrong PID/remote/second client/partial/timeout/cancel corpus | Wx unverified |
| R9.1 | Functioning cleanup after acquisition failure restores exact baseline. | `cleanup::Ledger`, fault injection | private Linux success and cancellation baselines | Linux pre-exec, malformed ancillary, deadline, and auto-reap restore baselines; persistent terminal lifecycle faults report retained incomplete ownership and are excluded from restoration claims | private Linux source-tree subset exercised bare-metal AMD64 through `98b7163`; complete Nth-operation and All release-target evidence unverified |
| R10.1 | Instrumentation and benchmarks prove real-time negative properties. | `tests/rt`, benchmarks, CI artifacts | allocator/syscall/lock counters zero | deliberate instrumentation tripwires | All unverified |
| R12.1 | Functioning cleanup restores native fd/map/path/pidfd/child, VM/port/child, or handle/view/pipe/Job/tree baseline. | backend cleanup suites | private Linux fd/map/path/pidfd/task/child/zombie cycles and stress baselines | private Linux injected forward failures restore baselines; terminal cleanup faults intentionally retain ownership until isolated process exit and do not count as restoration; full Nth suite planned | private Linux source-tree subset exercised bare-metal AMD64 through `98b7163`; 10,000-cycle exact-release and all other native targets unverified |
| R14.1 | Compatibility features neither weaken vNext nor appear in conformance examples. | migration feature policy/docs | migration examples use vNext | API/docs scan for legacy escape use | portable planned |
| R16.1 | Every normative MUST has both positive and negative traceability. | this file + CI trace checker | `planned::trace::all_requirements_linked` | missing/stale test/evidence link rejects release | release blocked |

## Implementation progress (not release evidence)

| Phase | Invariant | Local evidence | Independent review | State |
| --- | --- | --- | --- | --- |
| 0 | Feasibility, claim boundaries, MUST inventory, and ordered plan | macOS Arm64 0.4 baseline plus primary-source mechanism review | no unresolved finding | implemented; native vNext probes unverified |
| 1 | Private facade backends, required crate direction, application-neutral manifest, no safe native-parts escape | host fmt, strict Clippy, all-feature/no-default tests, compile-fail docs, warning-free rustdoc, and four cross-target checks; superseded 0.4 package evidence remains reproducible from its historical commit and `v0.4.0` tag | no unresolved finding after three review rounds | implemented locally; the excluded duplicate 0.4 source package was retired after private-backend consolidation, while its published artifact and tagged source remain available; exact native targets unverified |
| 2a | Public private/prepared ownership states and guarded runtime access boundary | exact commit `61d11f6`: all five native jobs, strict quality, Miri, ASan, fuzz, and policy green in [Actions 29175446059](https://github.com/ro-ag/native-ipc-rs/actions/runs/29175446059) | no unresolved finding after pedantic unsafe review | implemented; native activation remains batch-gated and full vNext conformance unverified |
| 3a | Private transaction-owned mixed-region and exact keyed-set scaffold | host count/direction/duplicate/aggregate/exact-commit/drop and Send/Sync tests | no unresolved finding after three review rounds | internal only; public session commit deliberately blocked on native state machine |
| 4a | Finite checked limits, unforgeable target-capability representation, and one reusable monotonic absolute deadline | exact commit `068e508`: all five native jobs, strict quality, Miri, ASan, fuzz, and policy green in [Actions 29175823735](https://github.com/ro-ag/native-ipc-rs/actions/runs/29175823735); limit/capability/deadline host corpus | no unresolved finding after two review rounds | portable foundation only; native atomic fact discovery/publication and hostile transport timing remain unverified |
| 4b | Canonical two-sided HELLO/ACCEPT/REJECT wire and payload-bound one-shot negotiated transcript | exact fix commit `8920f36`: all five native jobs and auxiliary gates green in [Actions 29176564979](https://github.com/ro-ag/native-ipc-rs/actions/runs/29176564979); dependency policy; platform-independent golden SHA-256 vector; opaque empty/bounded payload, full-frame truncation, every reserved byte, feature/minimum/atomic/target/digest/substitution/replay/order corpus | no unresolved finding after four codec rounds plus one readiness-boundary review | private portable codec only; authenticated exact-child transport, poisoning, public ready typestate, and later transaction binding remain unverified |
| 4c | Authentication receipts cannot separate from native transport; receiver-capable I/O excludes coordinator-only child control | exact commit `a8cc65a`: all five native jobs and auxiliary gates green in [Actions 29176784443](https://github.com/ro-ag/native-ipc-rs/actions/runs/29176784443); receipt mismatch, invalid facts, non-Clone and Send/!Sync assertions | no unresolved facade finding after two rounds; three Linux lifecycle probe rounds identified and documented blockers | private unreachable facade only; channel receipt adapters, executable image identity/durable reaping, macOS parent ACK, and native absolute-deadline I/O remain unverified |
| 4d | Bounded opaque application-control wire with independent exact sequences and transaction exclusion | exact commit `d02d1e5`: all five native jobs and auxiliary gates green in [Actions 29177587806](https://github.com/ro-ag/native-ipc-rs/actions/runs/29177587806); fixed-header validation before payload exposure or peer-declared second allocation, interleaved duplex, exact maximum, mutation/truncation/replay/future/sequence/transaction/guard-drop corpus | no unresolved finding after three review rounds | private portable codec only; authenticated native record I/O and public Ready-only control remain unverified |
| 4e | Exact active-mapping resource charging, shared poison state, and recoverable close accounting | exact commit `bee23e4`: all five native jobs, strict quality, Miri, ASan, fuzz, and policy green in [Actions 29178204449](https://github.com/ro-ag/native-ipc-rs/actions/runs/29178204449); reader/writer exact-length, mismatch/poison rollback, concurrent/destructor-blocked close, owner-drop orphaning, and panic-path corpus | no unresolved critical/high finding after three adversarial review rounds | private portable ownership scaffold only; native mapping adapters, public session close/abort, child lifecycle, and native leak evidence remain unverified |
| 5a | Anonymous nonblocking Linux packet transport with exact credentials and immediate bounded fd ownership | exact commit `52aa47d`: native Linux AMD64/Arm64 plus all other target/auxiliary gates green in [Actions 29178571963](https://github.com/ro-ag/native-ipc-rs/actions/runs/29178571963); exact 0/1/2/16-fd, short/wrong-peer/extra/truncated cleanup, and actual post-exec child/pidfd corpus | no unresolved critical/high finding after two adversarial review rounds | private unreachable primitive; image identity, absolute-deadline polling, process owner, memfd direction ordering, batch state machine, and public API remain unverified |
| 5b | Linux packet I/O and peer-exit waits share one absolute deadline and inseparable pidfd/credential/socket proof | exact fix commit `d956292`: native Linux AMD64/Arm64, ASan, all other target/auxiliary gates green in [Actions 29178906782](https://github.com/ro-ag/native-ipc-rs/actions/runs/29178906782); silence, saturated `POLLOUT`, injected EINTR/late send/late receive, persistent poison, pidfd exit wake, and ACK-held actual child exchange corpus | no unresolved critical/high finding after four adversarial review rounds plus lifecycle-race fix review | private unreachable mechanism; authenticated facade adapter, image/process owner, batch state machine, and public API remain unverified |
| 5c | Linux execute-authority limit is characterized and normatively explicit | exact native run [29179189298](https://github.com/ro-ag/native-ipc-rs/actions/runs/29179189298) proved uncontained executable upgrade; exact MDWE characterization commit `7744e0b` then proved both native architectures deny that upgrade inside the MDWE tree but permit fresh RX in [Actions 29179590235](https://github.com/ro-ag/native-ipc-rs/actions/runs/29179590235); broader pre-seal outside-tree delegation is accepted and awaits a native test; production still rejects before capability escape | review found and amendment includes the direction-specific delegation limit | normative blocker resolved; next invariant is structural trusted pre-exec MDWE proof, complete residual-authority corpus, and production preparation |
| 5d | Trusted pre-exec MDWE hook fails before exec and mints no premature security witness | exact commit `a1f3c7c`: native Linux AMD64/Arm64, ASan, and all auxiliary gates green in [Actions 29180257562](https://github.com/ro-ag/native-ipc-rs/actions/runs/29180257562) | first review rejected an unbounded child owner; corrected hook-only review clean | private scaffold only; exact image/channel/pidfd/bounded-cleanup composition and memory preparation remain unimplemented |
| 5e | Held Linux executable identity and pidfd evidence cannot mint after wrong/reaped image and survives path replacement | initial run [29180587125](https://github.com/ro-ag/native-ipc-rs/actions/runs/29180587125) exposed an early-exit test race on Arm64/ASan; exact fix commit `4f93fd6` uses bounded READY/release and is green on native Linux AMD64/Arm64, ASan, and all auxiliary gates in [Actions 29180802767](https://github.com/ro-ag/native-ipc-rs/actions/runs/29180802767) | no unresolved P1/P2 after ELF, PID-reuse, group-cleanup, CLOEXEC-slot-reuse, and deterministic-handshake reviews | private scaffold only; authenticated channel/receipt, bounded child owner, and memory preparation remain unimplemented |
| 5f | Atomic clone-time pidfd identity survives ignored SIGCHLD without numeric-PID reopening | test-only `clone3(CLONE_PIDFD)` probe; first native runs [29181515348](https://github.com/ro-ag/native-ipc-rs/actions/runs/29181515348) and [29181596240](https://github.com/ro-ag/native-ipc-rs/actions/runs/29181596240) exposed architecture/timing diagnostic variation; exact correction `ec90e0f` green native AMD64/Arm64, ASan, and all auxiliary gates in [29181676361](https://github.com/ro-ag/native-ipc-rs/actions/runs/29181676361) | no unresolved P1/P2 after process-global reaping, clone_args sizing, and teardown-diagnostic reviews | feasibility only; production clone3+MDWE+exec/error pipe, single pidfd owner, durable cleanup, receipt composition, and memory preparation remain unimplemented |
| 5g | Atomic held-image exec and durable exact-child ownership share the sole clone-time pidfd | exact implementation `e904e35`, corrected evidence tip `98b7163`: bare-metal AMD64 process corpus and focused syscall trace; live/stalled nonblocking Drop, explicit wait/terminate, cancel, exact SIGKILL/reap, `SIGCHLD=SIG_IGN`, `SA_NOCLDWAIT`, and broad waiter baselines; terminal cleanup faults prove bounded retained ownership and are excluded from baseline-restoration claims; all 10 hosted jobs green in [Actions 29187052061](https://github.com/ro-ag/native-ipc-rs/actions/runs/29187052061) | independent review found a persistent poll/waitid terminal-fault hot loop; a later hosted run exposed a one-shot child-list observation race in the test; both were corrected, including 25 direct no-default repetitions, with no unresolved finding | private unreachable mechanism only; authenticated HELLO, bootstrap-fd collision policy, production receipt composition, and physical Arm64 release evidence remain unverified |
| 5h | Linux private bootstrap deadlines, malformed ancillary ownership, and accepted pre-seal outside-MDWE delegation have adversarial native coverage | exact implementation `e904e35`, corrected evidence tip `3721cd3`: bare-metal AMD64 exact-mode/expired accept tests, expanded malformed-rights corpus, exact seal-state and outside-tree delegation characterization; corrected real-listener pressure is green on hosted native Linux AMD64/Arm64 and ASan in [Actions 29195512981](https://github.com/ro-ag/native-ipc-rs/actions/runs/29195512981) | independent review found descriptor ownership and simulated-listener gaps; both are corrected, but the real-listener physical-host rerun remains pending | private mechanisms only; authenticated batch/import/receipt binding, public memory authority, physical timing rerun, and physical Arm64 evidence remain unverified |
| 5i | Linux child-side fresh-session characterization plus exact direct-child cleanup never risks a reused numeric PGID | exact commit `861c139`: hosted native Linux AMD64/Arm64, ASan, and auxiliary gates green in [Actions 29196282000](https://github.com/ro-ag/native-ipc-rs/actions/runs/29196282000); post-exec SID/PGID, `setsid` failure, nonblocking Drop, auto-reap/broad waiter, and intentional ordinary/escaped descendant survival | two adversarial rounds proved that even a stopped leader is not a durable group identity under same-UID hostile signalling; numeric PGID cleanup was removed; final re-review has no P1/P2 | private unreachable mechanism only; no containment receipt or numeric group signal; section 9 ordinary-descendant termination conflicts with the threat model absent mandatory stronger containment; physical Arm64 release evidence unverified and release blocked |
| 5i-A | Linux production extraction atomically composes held-image exec, sole clone-time pidfd lifecycle, and one collision-safe inherited anonymous bootstrap endpoint without minting authority | exact commit `81832fd`: all ten hosted jobs green in [Actions 29197002887](https://github.com/ro-ag/native-ipc-rs/actions/runs/29197002887); success/path replacement/occupied slots/one inherited socket, object-identity closure, fixed stages, malformed/partial/silent/stall/deadline, repeated Drop/panic, exact children/ECHILD and resource baselines | three adversarial rounds resolved loader fd-slot reuse, missing exact-child evidence, and parallel PID/baseline races; final re-review has no P1/P2 | private unreachable pre-authentication owner only; no HELLO, credential binding, receipt, descriptor transfer, session, or memory authority; physical Arm64 release evidence unverified |
| 5i-B | Linux variable zero-rights seqpacket records have one verified native ceiling and preserve exact credential/deadline/poison semantics | exact commit `ad4ca15`: all ten hosted jobs, including native Linux AMD64/Arm64 and ASan, green in [Actions 29197506559](https://github.com/ro-ag/native-ipc-rs/actions/runs/29197506559); 1/exact-64-KiB, pre-I/O max+1 rejection, queued oversize consumption, injected-right closure, wrong credentials, silence/saturation/EINTR/late send+receive, and exact inherited-peer pidfd exit wake | independent review corrected the peer-exit topology fixture; final re-review has no P1/P2 | private unreachable transport primitive only; no HELLO codec, receipt, session, descriptor authority, or physical Arm64 release evidence; failed run `29197362446` is not evidence |
| 5i-C | Linux native atomic discovery uses compiler width facts plus checked runtime page/cache-line facts and proves cross-process publication | exact commit `d3ee93b`: all ten hosted jobs, including native Linux AMD64/Arm64 and ASan, green in [Actions 29197989720](https://github.com/ro-ag/native-ipc-rs/actions/runs/29197989720); sysconf equality; aligned MAP_SHARED u32/u64 Release/Acquire publication+ack; invalid/unavailable/overflow/unsupported corpus; bounded exact-pidfd outer watchdog; successful-path exact inner reap/ECHILD and fd/map/child baselines | review added bounded timeout teardown and narrowed forced-parent-death claims; final re-review has no P1/P2 | private discovery only; forced parent-death inner cleanup is excluded from exact-reap claims; no HELLO/session/receipt/memory authority; macOS/Windows implementations and evidence unverified; hosted Linux Arm64 is VM characterization, not physical Arm64 release evidence |
| 5i-D | Exact held child/image, sole clone pidfd, directional kernel credentials, native atomic discovery, and two canonical HELLOs yield only a private negotiating owner under one coordinator deadline | exact commit `0ae24f9`: all ten hosted jobs, including native Linux AMD64/Arm64 and ASan, green in [Actions 29199137594](https://github.com/ro-ag/native-ipc-rs/actions/runs/29199137594); empty/1/65,312-byte bidirectional payloads, fresh nonzero nonce, normal directional credentials, post-confirmation silence deadline, post-Coordinator-HELLO peer exit, exact resource/child/ECHILD baselines, all-feature/no-default workspace tests/doctests, and strict Clippy; the real-ID/effective-ID split is privileged Arm64 seccomp-unconfined Docker characterization only | independent review corrected automatic credential semantics from effective to real UID/GID and moved locally invalid feature offers before entropy/clone; initial commit `2f21c59` run [29198888250](https://github.com/ro-ag/native-ipc-rs/actions/runs/29198888250) failed only ASan because the test harness published the escaped descendant PID before `setsid`; `0ae24f9` adds a bounded exact-session wait and the final re-review has no P1/P2 | private mechanism checkpoint only; hosted Linux Arm64 is VM characterization, not physical release evidence; no ACCEPT/READY/session/receipt/fd/control/batch/memory authority; physical Arm64, packaged crate, and exact release commit unverified |
| 5i-E | Stored-deadline challenged Coordinator decision followed by explicit challenge-bound Receiver decision yields only private Accepted Linux owners after bilateral exact ACCEPT | protocol implementation `2801b47`; exact final SHA `b768979ce74be7a2063914cf9b27e17deb520d6f` is green in all ten jobs in [Actions 29201467797](https://github.com/ro-ag/native-ipc-rs/actions/runs/29201467797): Rust 1.97 native Linux AMD64/Arm64 all-feature/no-default/native permission tests, Linux AMD64 ASan, macOS Arm64 and Windows AMD64/Arm64 target checks, Miri, fuzz, policy, and quality gates; local privileged Arm64 Docker remains emulation-only characterization | audit resolved prequeued-decision causality with the 128-bit challenge, split receiver decision validation from application choice, and made zero local Reject unrepresentable; final SHA includes mapping-specific ASan oracle fix `a6df424` and pidfd publication fix `b768979`; failed runs [29201058759](https://github.com/ro-ag/native-ipc-rs/actions/runs/29201058759) and [29201262157](https://github.com/ro-ag/native-ipc-rs/actions/runs/29201262157) are non-evidence; final combined review has no P1/P2/P3 | private mechanism only; Accepted is not public Ready and exposes no receipt/session/fd/control/batch/mapping/memory authority; duplicate decision after valid transition deferred to common accepted dispatcher; physical user-owned Arm64, packaged crate, public session composition, and whole-vNext release completion unverified |
| 5i-F | Bilateral Accepted states consume into private role-asymmetric evidence owners without exposing separable native proof | exact implementation SHA `a63a06ea34fdb2389abe1791bc636741b3018e15`; all ten jobs green in [Actions 29202485515](https://github.com/ro-ag/native-ipc-rs/actions/runs/29202485515): Rust 1.97 native Linux AMD64/Arm64 all-feature, no-default, and native tests; Linux AMD64 ASan; macOS Arm64 and Windows AMD64/Arm64 general jobs; Miri, fuzz, policy, and quality; privileged seccomp-unconfined Arm64 Docker all/no-default plus 25 focused repetitions are characterization only | adversarial audit removed the stale symmetric generic receipt, enforced coordinator/receiver evidence asymmetry, terminal one-shot bilateral transcript facts, inseparable resource ownership, and immediate cleanup on the tested live-deadline invalid-evidence path; deadline/terminal-incomplete cleanup preserves durable exact authority; final review has no P1/P2/P3 | coordinator evidence owns exact lifecycle pidfd, held image, endpoint/transcript, and identity while receiver evidence owns authenticated spawning-parent endpoint/transcript facts without coordinator image/pidfd proof; owners are Send, non-Sync, non-Clone, with no raw parts; no public Ready/session/control/fd transfer/batch/mapping/memory authority, symmetric sole-writer/image proof, immutable ongoing image attestation, or revocation; macOS/Windows Phase 5i-F, physical user-owned Arm64, packaged crate, whole-vNext, release-candidate, and release completion remain unverified |
| 5i-G0 | A private role-neutral `AcceptedControlDispatcher` owns portable accepted control sequence, transaction, transport, and persistent-poison state; Linux negotiation advertises the truthful one-packet application payload maximum of 65,464 bytes (65,536-byte packet minus 72-byte header) | exact implementation `9eae30f6f2e1a6083459fd6e34bec537387540e0`; all ten jobs are green in [Actions 29203533427](https://github.com/ro-ag/native-ipc-rs/actions/runs/29203533427), including native hosted Linux AMD64/Arm64 and ASan plus the non-Linux/auxiliary gates; local macOS Rust 1.97 all-feature/no-default tests, doctests, and strict Clippy are green; privileged seccomp-unconfined Arm64 Docker passed proportional gates but is emulation/characterization only | adversarial review added transaction hooks, single-allocation owned decode, oversized-success and poll-error coverage, and reconciled the stale header-before-read wording with the normative bounded-allocation rule and Linux one-`recvmsg`/no-`MSG_PEEK` design | scaffold only: one independently bounded record is received and its allocation reused; sequences are independent, transaction interleaving and ambiguous/hostile paths poison persistently; no Linux authenticated evidence-owner/native adapter wiring, native `SCM_RIGHTS` control transfer, public Ready/session/control API, batch/mapping/memory authority, physical Arm64, packaged-crate, exact-release, or release evidence; hosted Linux Arm64 is an Azure VM/native runner, not physical user-owned Arm64 |
| 5i-G1a | Consuming role-scoped Accepted Linux evidence yields private authenticated application-control owners without strengthening receiver lifecycle claims | exact implementation `a26e1df71fccf44540a174a4994cd421668b5700`; all ten jobs are green in [Actions 29204483739](https://github.com/ro-ag/native-ipc-rs/actions/runs/29204483739), including native hosted Linux AMD64/Arm64 and ASan plus non-Linux/auxiliary gates; local macOS Rust 1.97 gates are green; privileged seccomp-unconfined Arm64 Docker full/focused gates are green characterization only | independent final review has no issue; native corpus covers empty/exact-65,464-byte duplex, original caller deadlines, exact credentials, injected-right closure, queued preconstruction record, replay/truncation/oversize/wrong-peer/silence, live-empty versus HUP EOF, and terminal post-consumption poll EINTR without a second receive | coordinator alone owns singular pidfd/image/evidence/endpoint and `OwnedChildLifecycle`; receiver owns spawning-parent evidence/endpoint only and observes HUP as `ExitedUnknown`, never parent pidfd/image/exit code; one bounded `recvmsg`, no `MSG_PEEK`, persistent poison; private G1a only with no public Ready/session/control API, batch `SCM_RIGHTS`, transaction/mapping/memory authority, endpoint revocation, physical Arm64, packaged/release, or whole-vNext evidence; hosted Linux Arm64 is not physical user-owned Arm64 |
| 5i-G1b | The inseparable accepted owner admits one coordinator-initiated exact manifest-bound capability record while retaining every received fd inside a terminal transaction guard | exact implementation `c34e904f2451907a3f35c7b17cf79636c19187aa`; all ten jobs are green in [Actions 29205870709](https://github.com/ro-ag/native-ipc-rs/actions/runs/29205870709), including hosted native Linux AMD64/Arm64 and ASan plus non-Linux/auxiliary gates; local macOS Rust 1.97 gates and privileged seccomp-unconfined Arm64 Docker full/focused gates are green, with Docker classified only as container/emulation characterization | adjacent corpus covers role-trait separation, canonical native/application magic disjointness, exact caller deadline, 0/1/2/16 rights, truncation, wrong credentials, nonce/transaction/manifest substitution, same-guard replay with one native I/O and retained duplicate input, application interleaving, hostile queued traffic, first native-operation failure, fd baselines, transaction-owned cleanup, and persistent poison; independent review P3 was fixed and focused re-review reports no P1/P2/P3 | private terminal transport/ownership checkpoint only; no descriptor extraction, transaction completion, IMPORTED/SEALED/READY/COMMIT, memory import/mapping/activation, public Ready/session/control, child/map baseline claim for this no-child/no-map slice, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1c | Accepted evidence transfers immutable session provenance and limits into the inseparable owner, which alone validates canonical entries and mints monotonic transaction IDs and capability frames | exact implementation `bd4e2789cdcf218e5569529003f975e4c7c41e1b`; all ten hosted jobs are green in [Actions 29206612933](https://github.com/ro-ag/native-ipc-rs/actions/runs/29206612933), including native Linux AMD64/Arm64, ASan, non-Linux targets, and auxiliary gates; local macOS Rust 1.97 formatting, strict Clippy, all-feature/no-default tests and warning-free docs pass; privileged seccomp-unconfined Rust 1.97 Arm64 Docker passes full gates as container/emulation characterization only | adjacent corpus covers exact accepted facts, internal nonce/PID/transaction construction, monotonic IDs, exhaustion poisoning, nonce/PID/transaction/count substitution, duplicate/count/per-logical-region/aggregate-logical-and-mapped-limit rejection, page-rounded mapped length at the logical maximum, expired-deadline rejection before native I/O or ID consumption, and Nth-operation terminal failure while retaining the complete G1b transport/cleanup/poisoning corpus; independent review P2 corrected mapped-length limit semantics and final re-review reports no P1/P2/P3 | private provenance/allocation checkpoint only; no caller-built frame, raw endpoint, or duplicated lifecycle authority, but also no production completion, prepared-memory/expected-batch adapter, IMPORTED/SEALED/READY/COMMIT, mapping/activation, public Ready/session/control, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1d | Role-scoped accepted Linux evidence selects one exact session-wide MDWE/residual-authority profile that the dispatcher binds into every canonical capability frame | exact implementation `a48a051abbce0414fbe6bf00d26888ceba3e6f2b` is green across all ten jobs in [Actions 29207032792](https://github.com/ro-ag/native-ipc-rs/actions/runs/29207032792); local macOS Rust 1.97 formatting, strict Clippy, all-feature/no-default tests and warning-free docs pass; privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass as container/emulation characterization only | profile 1 exact encoding, legacy-profile construction rejection before I/O, peer profile substitution with exact frame mismatch and retained fd cleanup, and every-field mutation at byte 76 are covered; independent adversarial review reports no P1/P2/P3 | policy/provenance checkpoint only; profile states accepted Linux limits but proves no individual memfd seal/import/map state and adds no prepared batch, IMPORTED/SEALED/READY/COMMIT, activation, public API, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1e | Coordinator transaction consumes and retains one portable prepared batch and derives canonical entry metadata internally instead of accepting production caller-built entries | exact implementation `c8c9558ac714d0e34014c34f939034294a868632` is green across all ten jobs in [Actions 29207363955](https://github.com/ro-ag/native-ipc-rs/actions/runs/29207363955); local macOS Rust 1.97 full gates pass; privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass as container/emulation characterization only | adjacent corpus covers 1/2/4/16 reverse-ordered mixed-direction batches; exact ID/incarnation/logical/mapped/writer/access/ordinal fields; empty rejection before I/O/ID consumption; and poison-before-prepared-resource-destruction on abandonment and send failure; independent review P3 was fixed and focused re-review reports no P1/P2/P3 | native capability collection is still a separate internal send input, so exact fd/object/seal/ordinal preparation and substitution resistance remain unimplemented; no receiver ExpectedBatch/import, IMPORTED/SEALED/READY/COMMIT, activation, public API, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1f-a | An all-coordinator-writer portable batch consumes into one canonical Linux memfd batch with exact final seals, same-object export descriptors, retained writer mappings, stored deadline, and complete pending cleanup ownership | exact implementation `f94c93f7273fb7a9f33779c1a9aba90817e0b2a4` plus scoped mapping-baseline correction `1c8a3ad4bcde2c5d555d31a96949d51bb19f55f1` are green across all ten hosted jobs in [Actions 29210607343](https://github.com/ro-ag/native-ipc-rs/actions/runs/29210607343); local macOS Rust 1.97 full gates pass; privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass as container/emulation characterization only | 1/2/4/16 reverse-order canonical batches preserve every initialized logical byte and exact metadata/ordinals; wrong profile/direction/object substitution, expired deadline, future writable mapping/pwrite/resize denial, pending clear verification, and isolated first/middle/final fd/vNext-map baseline restoration are covered; independent exact-byte P3 was fixed and final re-review reports no P1/P2/P3 | coordinator-writer preparation only; physical/native-host and release evidence absent; no accepted send in this commit, receiver-writer, receiver import, IMPORTED/SEALED/READY/COMMIT, activation, or public API |
| 5i-G1f-b | The same accepted coordinator owner retains and sends its canonical prepared memfd batch without accepting a caller fd slice or replacement deadline | exact implementation `4e8231bbeadb1d38ca2525b9baf78df9c3526026` is green across all ten hosted jobs in [Actions 29210656133](https://github.com/ro-ag/native-ipc-rs/actions/runs/29210656133); local macOS Rust 1.97 full gates pass; privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass as container/emulation characterization only | real accepted exact-child 1/2/4/16 transfer verifies initialized peer reads, final seals, writer-map denial, exact transport framing/credentials/rights, persistent poison after terminal drop, poison-before-native-cleanup on abandonment/revalidation failure, and fd/task/child baselines; deadline mismatch fails before transaction and ordinary application control remains usable; independent review closed two P2 production substitution paths and one P3 ordering-evidence gap, with final re-review reporting no P1/P2/P3 | terminal coordinator-writer transport only; receiver expected metadata is test-coordinated, not a production ExpectedBatch/import adapter; no receiver-writer, IMPORTED/SEALED/READY/COMMIT, activation, public API, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1g | The accepted receiver consumes one pre-receipt canonical ExpectedBatch and inseparably validates/imports the coordinator-writer native batch into terminal read-only pending ownership | dependency commit `9b00e6813abdb81ecb4352c60647fc0b569a6c42` and exact accepted-import implementation `9e81e3f47d883aa9f9c18767fb6aecfdf3c2a481` are green across all ten hosted jobs in [Actions 29211825345](https://github.com/ro-ag/native-ipc-rs/actions/runs/29211825345); local macOS Rust 1.97 and privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass, with Docker as container/emulation characterization only | canonical decode plus session/transaction/profile/limit/ID/incarnation-shape/writer/access/logical/mapped/ordinal/flag/total substitution corpus; exact-child 1/2/4/16 full-byte reads and final seals; wrong credentials/0/1/2/N rights/truncation inherited from the same one-recv transport; local direction/active-limit/expiry rejection; first/middle/final invalid-object and post-mmap advisory failures retain all installed fds/partial or pending mappings until poison and restore fd/vNext-map/child baselines; independent review corrected active-limit and partial-import ownership P2s, including address-zero mmap ownership, and final re-review reports no P1/P2/P3 | coordinator-writer terminal import only with fresh-session active-limit preflight but no ongoing active ledger; no receiver-writer, IMPORTED/SEALED/READY/COMMIT, activation/runtime exposure, public ExpectedBatch/session API, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1h | The inseparable accepted owner completes exact full-manifest Linux receiver-writer IMPORTED/SEALED preparation while withholding all runtime authority | exact implementation `3914dc18d6e3efcefdbc2f28487563932cb06703` is green across all ten hosted jobs in [Actions 29213561283](https://github.com/ro-ag/native-ipc-rs/actions/runs/29213561283), including native Linux AMD64/Arm64 and Linux AMD64 ASan; local macOS Rust 1.97 and privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass, with Docker as container/emulation characterization only | homogeneous 1/2/4/16 receiver-writer batches preserve full logical bytes; coordinator mappings are destroyed before escape; receiver RW maps precede final seals; after IMPORTED the coordinator attenuates every escaped fd before any RO mapping and continues best-effort across a seal failure; new writable maps fail after sealing; every receipt truncation plus exact/stale/duplicate IMPORTED/SEALED, application-kind interleaving, continuous wrong-kind traffic, wrong credentials, 1/2/16 injected rights, deadline silence, invalid objects, Nth seal failures, post-mmap receiver/coordinator advice failures, and final-seal revalidation failure poison and restore fd/vNext-map/task/child baselines; independent review corrected complete-batch attenuation and expanded post-mapping/replay evidence, and the final re-review reports no P1/P2/P3 | terminal homogeneous receiver-writer preparation only; no mixed-direction reducer, READY/COMMIT, ongoing active ledger, activation/runtime exposure, public APIs, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1i-a | One private coordinator owner consumes and canonically retains an arbitrary mixed-direction Linux native batch without exposing raw parts | exact implementation `ba13372623dff7a29bd6be1e95f6fdee3c2676c0` is green across all ten hosted jobs in [Actions 29213948297](https://github.com/ro-ag/native-ipc-rs/actions/runs/29213948297), including native Linux AMD64/Arm64 and Linux AMD64 ASan; local macOS Rust 1.97 and privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass, with Docker as container/emulation characterization only | 1/2/4/16 reverse-input alternating-direction batches preserve exact bytes, per-direction seals/access, canonical capability object identity and ordinals; profile/expiry, retained coordinator mapping, and first/middle/final preparation failures restore exact fd/vNext-map baselines; owner is Send, non-Sync, non-Clone; independent review corrected capability-order and revalidation evidence, and final re-review reports no P1/P2/P3 | native preparation only; no accepted send, mixed receiver expectation/import, full-batch IMPORTED/SEALED attenuation, READY/COMMIT, active ledger, runtime/public API, physical Arm64, packaged, release, or whole-vNext evidence |
| 5i-G1i-b | One pre-receipt mixed receiver expectation consumes every installed descriptor into one direction-aware pending or failure owner | exact implementation `e9fbf60b7e42bc0d1cc656c0779743473f370c14` is green across all ten hosted jobs in [Actions 29214396874](https://github.com/ro-ag/native-ipc-rs/actions/runs/29214396874), including native Linux AMD64/Arm64 and Linux AMD64 ASan; local macOS Rust 1.97 and privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates pass, with Docker as container/emulation characterization only | 1/2/4/16 mixed imports preserve exact bytes and direction-specific seals/mappings; every independent negotiated count/region/logical/mapped/active limit branch, stored-deadline expiry, wrong manifest, duplicate/wrong objects, invalid ordinals 1/2/4/16, advice failures 1/17/32, owner drop order, and exact success/failure fd/vNext-map baselines are covered; independent review corrected limit-branch and deadline test precision, and final re-review reports no P1/P2/P3 | private native import only; no accepted credentials/rights transport, full-batch IMPORTED/SEALED attenuation, READY/COMMIT, active ledger, runtime/public API, physical Arm64, packaged, release, or whole-vNext evidence |
| Linux legacy retirement | Only the quiescent memfd allocator shared by the public memory facade and vNext remains in `backend/linux.rs`; obsolete filesystem bootstrap, stream framing, single-region transfer, and blocking child ownership are absent | exact implementation `7f47b57cc19ad45da9d983f8587aec05a43f2622` is green across all ten hosted jobs in [Actions 29222429002](https://github.com/ro-ag/native-ipc-rs/actions/runs/29222429002), including native Linux AMD64/Arm64, Linux AMD64 ASan, macOS Arm64, Windows AMD64/Arm64, Miri, fuzz, policy, and quality; local macOS full gates, three packaged crates, and privileged seccomp-unconfined Rust 1.97 Arm64 Docker full gates also pass | `docs/dead-code-audit.md` maps all 53 remaining suppressions and 232 `cfg(test)` gates; retained Linux allocation has a local `deny(dead_code)` boundary; independent adversarial diff review reports no P1/P2/P3 | cleanup only; no new mixed reducer, attenuation, READY/COMMIT, activation, public API, native permission claim, physical Arm64, or release evidence |

## Native release evidence ledger

| Target | Exact release SHA | Source-tree conformance | Packaged-crate conformance | Leak/fault/RT suites | Status |
| --- | --- | --- | --- | --- | --- |
| Linux GNU AMD64, kernel 6.3+, glibc 2.31+ | — | — | — | — | unverified |
| Linux GNU Arm64, kernel 6.3+, glibc 2.31+ | — | — | — | — | unverified |
| macOS Arm64 | — | — | — | — | unverified |
| Windows AMD64 | — | — | — | — | unverified |
| Windows Arm64 | — | — | — | — | unverified |

The existing green 0.4.0 CI run predates vNext and is not evidence for this
matrix. Release remains blocked while any row or target is planned, blocked,
or unverified.
